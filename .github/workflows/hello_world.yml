name: Hello World

on:
  workflow_dispatch:

jobs:
  saludar:
    runs-on: ubuntu-latest
    steps:
      - name: Decir hola
        run: echo "¡Hola! Este es mi primer workflow de GitHub Actions 🎉 Modificado"
